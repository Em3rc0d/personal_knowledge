# MK0 — Transferred UNKNOWNs

Status: **EXPLICITLY TRANSFERRED FORWARD**

These items did not block Mine & Frame closure because they require normalization, operationalization or execution evidence rather than additional source discovery.

## Runtime / reproducibility

- per-notebook executable dependency proof;
- exact runtime behavior under current model/provider versions;
- current SDK migration details for legacy examples;
- complete external API compatibility receipts.

## Security / containment

- project-specific sandbox effectiveness;
- tenant isolation and secret isolation under production load;
- exact authenticated browser mutation reachability for every browser-capable example;
- complete dispatcher enforcement for every consequential tool;
- complete data-retention behavior of every external service.

## Side effects / retries

- precise retry and idempotency semantics for every tutorial;
- unknown-outcome handling after timeout for every mutating action;
- compensating-action behavior where idempotency is unavailable;
- exact lower-level DataScribe mutation dispatcher and database permission enforcement;
- universal approval coverage for HR outbound messaging.

## Memory / epistemics

- quality metrics for each memory family;
- memory write validation and poisoning resistance across implementations;
- retention/deletion policies for long-running stores;
- persistent improvement evidence for systems labelled `self-improving`.

## Multi-agent

- benchmarked benefit versus single-agent/deterministic baselines;
- coordination cost under realistic load;
- failure containment when one worker produces poisoned or invalid state.

## Operations

- system-specific deployment/rollback evidence;
- SLO/SLA behavior;
- observability completeness;
- production incident/recovery evidence.

## Ownership by later MKs

```text
MK1
  classify unknowns and make missing dimensions explicit

MK2
  convert material unknowns into required contracts/checklists/test obligations

MK5+
  close claims that require repeated execution, adverse fixtures or real-system certification
```

`UNKNOWN` is a valid engineering state. It must not be replaced by a plausible assumption merely to complete a classification record.
