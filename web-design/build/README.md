# Web Design — Build

Build will hold operational artifacts derived from closed design knowledge.

Candidate future artifacts:

```text
schemas/
  design-contract.schema.*
  provenance.schema.*
  component-contract.schema.*

templates/
  DESIGN.md
  component-contract.md
  visual-evidence.md

rulesets/
  design-lint.*
  provenance-lint.*
```

MK0 intentionally does **not** claim these artifacts are complete yet. Build begins only after the relevant semantics are sufficiently closed.
